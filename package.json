{
  "name": "hik-camera-server",
  "version": "1.0.0",
  "description": "HTTP server for handling HIK camera vehicle detection events",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "morgan": "^1.10.0"
  }
}